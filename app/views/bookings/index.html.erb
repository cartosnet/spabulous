<div class="container">
  <div class="row">
    <div class="col-xs-6">
      <h3>Vos réservations</h3>
      <% @bookings.each do |booking| %>
        <p> You have a booking for the: <%= booking.started_at.strftime('%F   %H:%M h') %></p>
        <p> <%= link_to "Details of the booking", flat_booking_path(booking.flat, booking) %></p>
        <hr>
      <% end %>
    </div>
    <div class="col-xs-6">
      <h3>your flats</h3>
      <%= render 'flats/all' %>
    </div>
  </div>
</div>

